name: 构建签名APK并发布Release
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin
          cache: gradle

      - run: chmod +x gradlew

      - name: 构建APK
        run: ./gradlew assembleRelease

      - name: 签名（用回最兼容的插件，不折腾新插件）
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.ALIAS }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "34.0.0"

      - name: 上传签名APK
        uses: actions/upload-artifact@v4
        with:
          name: apk
          path: app/build/outputs/apk/release/*-signed.apk

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: apk
          path: apks

      - name: 北京时间版本号
        id: build_time
        run: |
          echo "tag=build-$(date -d '+8 hour' +'%Y%m%d-%H%M%S')" >> "$GITHUB_OUTPUT"
          echo "name=自动构建 $(date -d '+8 hour' +'%Y年%m月%d日 %H:%M')" >> "$GITHUB_OUTPUT"

      - name: 发布Release
        uses: softprops/action-gh-release@v2
        with:
          files: apks/*.apk
          tag_name: ${{ steps.build_time.outputs.tag }}
          name: ${{ steps.build_time.outputs.name }}
          body: 已签名可直接安装
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
